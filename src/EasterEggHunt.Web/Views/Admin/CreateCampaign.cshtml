@model EasterEggHunt.Web.Models.CreateCampaignViewModel
@{
    ViewData["Title"] = "Neue Kampagne erstellen";
}

<div class="row">
    <div class="col-md-8">
        <h2>Neue Kampagne erstellen</h2>
        <p class="text-muted">Erstellen Sie eine neue Easter Egg Hunt Kampagne</p>

        <form asp-action="CreateCampaign" method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>

            <div class="mb-3">
                <label asp-for="Name" class="form-label"></label>
                <input asp-for="Name" class="form-control" placeholder="z.B. Oster-Kampagne 2024" />
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>

            <div class="mb-3">
                <label asp-for="Description" class="form-label"></label>
                <textarea asp-for="Description" class="form-control" rows="4" placeholder="Beschreiben Sie Ihre Kampagne..."></textarea>
                <span asp-validation-for="Description" class="text-danger"></span>
            </div>

            <div class="mb-3">
                <button type="submit" class="btn btn-primary">
                    <i class="fas fa-save"></i> Kampagne erstellen
                </button>
                <a asp-action="Index" class="btn btn-secondary">Abbrechen</a>
            </div>
        </form>
    </div>

    <div class="col-md-4">
        <div class="card">
            <div class="card-header">
                <h5 class="card-title mb-0">Tipps</h5>
            </div>
            <div class="card-body">
                <ul class="list-unstyled mb-0">
                    <li class="mb-2">
                        <i class="fas fa-lightbulb text-warning"></i>
                        <strong>Kampagnenname:</strong> Wählen Sie einen aussagekräftigen Namen
                    </li>
                    <li class="mb-2">
                        <i class="fas fa-lightbulb text-warning"></i>
                        <strong>Beschreibung:</strong> Erklären Sie das Ziel der Kampagne
                    </li>
                    <li class="mb-2">
                        <i class="fas fa-lightbulb text-warning"></i>
                        <strong>QR-Codes:</strong> Fügen Sie nach der Erstellung QR-Codes hinzu
                    </li>
                </ul>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
